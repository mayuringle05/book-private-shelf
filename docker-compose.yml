services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: private_shelf
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - private_shelf_pg:/var/lib/postgresql/data
volumes:
  private_shelf_pg:
